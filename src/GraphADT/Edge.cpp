#include "Edge.h"


GraphADT::Edge::Edge(size_t edge_number, size_t initial_node_id, size_t terminal_node_id){
    edge_number_ = edge_number;
    initial_node_id_ = initial_node_id;
    terminal_node_id_ = terminal_node_id;
};

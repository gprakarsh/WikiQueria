#include "Graph.h"

// Inserts a perfect tree of height 2 with starting node `start`
void insertBasicTree(Graph& g, size_t start);

// Inserts a cycle of size 4 with starting node `start`
void insertBasicCycle(Graph& g, size_t start);
